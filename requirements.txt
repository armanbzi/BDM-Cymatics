# Core
numpy
scipy
pyarrow

# Trusted zone — PySpark driver on host auto-uses Java 11 or 17; Spark cluster runs in Docker
pyspark>=3.5,<3.7

# MinIO object storage
minio

# Kafka streaming (hot path)
kafka-python

# Audio recording (warm & hot paths)
sounddevice

# Cymatics visualization (trusted zone + warm/hot preview)
opencv-python

# Environment & config
python-dotenv

# HTTP API access (Freesound cold path)
requests

# Delta Lake + KPI queries (data consumption)
deltalake
pandas

# Resource monitoring (orchestrator)
psutil

# ffmpeg is also required system-wide (apt install ffmpeg / brew install ffmpeg)
