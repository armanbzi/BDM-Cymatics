# Core
numpy
scipy
pyarrow

# Trusted zone — PySpark driver on host auto-uses Java 11 or 17; Spark cluster runs in Docker
pyspark>=3.5,<3.7

# MinIO object storage
minio

# Kafka streaming (hot path)
kafka-python

# Audio recording (warm & hot paths)
sounddevice

# Cymatics visualization (trusted zone + warm/hot preview)
opencv-python

# Environment & config
python-dotenv

# HTTP API access (Freesound cold path)
requests

# Delta Lake + KPI queries (data consumption)
deltalake
pandas

# Data consumption dashboard (Streamlit + Plotly)
streamlit
plotly

# Resource monitoring (orchestrator)
psutil

# Milvus vector database (exploitation-zone embeddings)
pymilvus>=2.4,<2.5

# Audio embeddings — PANNs CNN14 (2048-dim environmental sound classification)
torch
panns-inference

# Text embeddings — all-MiniLM-L6-v2 (384-dim sentence embeddings for RAG)
sentence-transformers

# Cymatics image embeddings — CLIP ViT-B/32 (512-dim visual pattern search)
transformers
Pillow

# Data governance — quality validation (Great Expectations)
great_expectations

# Classifier head training on top of Milvus embeddings (exploitation-zone)
scikit-learn
joblib
matplotlib
seaborn

# ffmpeg is also required system-wide (apt install ffmpeg / brew install ffmpeg)
